<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recuperar Senha</title>
  <link rel="stylesheet" href="/css/styleGlobal.css">
  <link rel="stylesheet" href="/css/styleComponents.css">
  <link rel="stylesheet" href="/css/pages/styleAuth.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body class="auth-background">
  <div class="auth-container">
    <div class="auth-logo">
      <img src="/img/dataVac logo ofc.png" alt="Dialoga">
    </div>

    <div class="auth-card">
      <div class="tabs">
        <a href="/" class="active">Entrar</a>
      </div>

      <h1 class="form-title">Esqueci minha senha</h1>
      <p class="form-subtitle">Digite seu e-mail para redefinir a senha:</p>

      <form class="auth-form" method="post" action="/recuperar">
        <div class="form-group">
          <label class="form-label">Email</label>
          <div class="input-icon-container">
            <input type="email" name="email" class="form-input" placeholder="Digite seu e-mail" required>
            <span class="input-icon">@</span>
          </div>
        </div>

        <div class="form-group">
          <label class="form-label">Nova Senha</label>
          <div class="input-icon-container">
            <input type="password" name="senha" class="form-input" placeholder="Nova senha" minlength="6" required>
            <span class="input-icon">🔒</span>
          </div>
        </div>

        <button type="submit" class="submit-button">Recuperar</button>
      </form>

      <p style="font-size: 11px; margin-top: 20px;">
        Lembre-se de usar uma senha segura e que você consiga lembrar.
      </p>
    </div>
  </div>

  <% if (typeof erroMessage !== 'undefined' && erroMessage) { %>
  <script>
      Swal.fire({
          icon: 'error',
          title: 'Erro ao recuperar senha',
          text: '<%= erroMessage %>',
          confirmButtonColor: '#d33'
      });
  </script>
  <% } %>
</body>
</html>
